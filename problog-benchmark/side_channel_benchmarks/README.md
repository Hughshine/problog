`probability-oopsla.txt` 的解读
prob for rules and facts. ignore the last number.

1. relation
2. Time


BV作为head的rule可以先忽略。如果只考虑.dl里面的facts，它们无关。目前应该只有这些rules需要computational equivalence.

prob 相同的赋予 ; 分裂出的子规则，也就是说这些子规则被认为概率独立

没有在.txt里标注概率的rule，默认为1

P2 就是空的